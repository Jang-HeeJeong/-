# 표준제조기준 개정 반영 가이드

식약처 고시가 개정됐을 때 **어디를 고치면 되는지** 정리한 문서입니다.
1년에 한 번 여는 프로젝트라 "무엇이 어디 있었는지" 다시 찾는 시간을 줄이는 게 목적입니다.

현재 기준: **고시 제2025-3호 (2025.1.16 시행)**

---

## 0. 시작 전에

```bash
python serve.py
```

1. `http://localhost:7777/tests.html` 을 열어 **먼저 전부 통과하는지 확인**합니다.
   (고치기 전 상태가 정상인지 확인해 둬야, 나중에 실패가 뜨면 내 수정 때문인지 알 수 있습니다)
2. 그다음 아래 표를 보고 해당 파일을 고칩니다.
3. 고친 뒤 다시 `tests.html`을 열어 **바뀐 항목만 실패하는지** 확인합니다.

---

## 1. 파일 구조

```
index.html                화면 (UI) — 개정 때는 거의 손대지 않습니다
app/표제기_데이터.js       ★ 성분·분량 데이터      ← 개정의 대부분이 여기
app/판정_규칙.js           ★ 배합 규칙·판정 로직   ← 규칙이 바뀌면 여기
tests.html                회귀 테스트 실행 화면
tests/                    테스트 케이스·기준값
```

> `app/표제기_데이터.js`는 JSON이 아니라 JS 파일입니다. 빌드 도구 없이 `<script>`로 읽기 위해서입니다.
> 맨 앞의 `var __EMBEDDED_DB__ = ` 뒤부터가 순수 JSON 구조입니다.

**⚠ 데이터 파일은 `app/표제기_데이터.js` 하나뿐입니다.**
예전에 `의약품_표준제조기준.json`이라는 파일이 있었지만 앱이 읽지 않는 옛 추출본이라 삭제했습니다.
비슷한 이름의 파일을 새로 만들어 두면 **그걸 고쳐도 아무 일도 일어나지 않으니** 주의하세요.

---

## 2. 개정 항목 → 고칠 위치

### 성분·분량이 바뀐 경우 → `app/표제기_데이터.js`

가장 흔한 개정입니다. **코드는 안 건드립니다.**

| 바뀐 내용 | 위치 (JSON 경로) |
|---|---|
| 비타민 성분 추가/삭제, 1일 최소·최대 분량 | `제1장_비타민미네랄.표.표1_비타민` |
| 미네랄 | `제1장_비타민미네랄.표.표2_미네랄` |
| 기타성분 (타우린·우르소데옥시콜산 등) | `제1장_비타민미네랄.표.표3_기타성분` |
| 생약 | `제1장_비타민미네랄.표.표4_생약` |
| 연령구분계수 | `제1장_비타민미네랄.표.표5_연령구분계수` |
| 효능효과 문구 | `제1장_비타민미네랄.표.표6_효능효과` |
| 제2·3·7·9장 유효성분 | `제N장_….표.표1_유효성분` |
| 제2·3·7·9장 연령구분계수 | `제N장_….표.표3_연령구분계수` |
| 제형 목록, 용법용량 문구, 효능효과 기재조건 | `제N장_….기준` |

**분량 표기 형식**은 기존 값을 그대로 따라 쓰세요. 파서(`parseAmountStr`)가 이 형식을 전제로 합니다.

```
"1일최대분량": "100 mg"          단일 값
"1일최대분량": "500(1000) μg"    괄호 = 상위 연령 상한 (ELEVATED_AGE_THRESHOLD와 연동)
"1일최대분량": "10/20 mg"        성분명이 배열일 때 순서대로 대응
"1일최대분량": "-"               제한 없음
```

**성분명은 고시 표기 그대로** 넣으세요. 앞의 `△` 같은 기호도 포함합니다
(예: 제7장 거담제는 `구아이페네신`이 아니라 `△구아이페네신`).

---

### 배합 규칙·연령 구분이 바뀐 경우 → `app/판정_규칙.js`

| 바뀐 내용 | 위치 |
|---|---|
| 연령 구간 자체가 바뀜 | `CH1_AGE_MIN_MONTHS`, `CH1_AGE_DISPLAY`, `CH1_UNIFIED_AGE_GROUPS` |
| 만 15세·19세 등 상위 연령 기준 | `DYNAMIC_AGE_MIN_MONTHS`, `DYNAMIC_AGE_DISPLAY` |
| 특정 성분의 연령 제한 (예: 타우린 만 15세 미만 불가) | `SPECIAL_INGR_AGE_RULES` |
| 괄호 상한이 적용되는 연령 (폴산·우르소 등) | `ELEVATED_AGE_THRESHOLD` |
| 마그네슘 염 환산계수 | `MG_SALT_FACTORS` |
| 만 8세 미만 금지 미네랄 | `MINERAL_UNDER8_BANNED` |
| 아스피린계 성분 목록 (만 15세 미만 불가) | `ASPIRIN_INGR` |
| 성인으로 취급하는 연령 키 | `ADULT_AGE_GROUPS` |
| 단위 환산 | `UNIT_TO_MCG`, `convertToUnit` |

**비율 규칙**(1/15, 1/5, 1/10 같은 것)은 상수가 아니라 검증 함수 안에 있습니다.

| 규칙 | 위치 |
|---|---|
| 제1장 항목별 합산 검증 | `checkItemSums` |
| 제2장 Ⅰ항 비례배합(1/2~3/2), Ⅲ항 1/5, Ⅴ항 1/15, Ⅳ항 배합최소량 | `validateChapter2` |
| 제3장 배합 규칙 | `validateChapter3` |
| 제7장 배합 규칙 | `validateChapter7` |
| 제9장 1회 최대 = 1일 최대 × 1/3 또는 1/6 | `validateChapter9`, `parseCh9CoeffStr` |

---

### 장이 추가·삭제된 경우

> **데이터는 이미 들어 있을 수 있습니다.** 현재 화면에는 제1·2·3·7·9장만 나오지만
> `app/표제기_데이터.js`에는 제4·5·6·8·10~14장 데이터도 담겨 있습니다.
> 이 장들을 쓰려면 아래 2~5번만 하면 되고, 1번은 건너뛸 수 있습니다.

1. `app/표제기_데이터.js`에 `제N장_…` 항목 추가 (이미 있으면 생략)
2. `app/판정_규칙.js`의 `CHAPTER_ORDER`에 키 추가
3. `validateChapterN` 함수 작성 (기존 장을 참고)
4. `index.html`의 `validateCurrentChapter`·`runValidation`·`isMatrixMode` 분기에 연결
5. `tests.html`의 `VALIDATORS` 표에 추가하고 `tests/cases.js`에 케이스 작성

> 이건 개정 중 유일하게 화면 코드까지 건드리는 작업입니다.

---

## 3. 고친 뒤 확인

`http://localhost:7777/tests.html` 을 새로고침합니다.

| 결과 | 뜻 |
|---|---|
| 전부 통과 | 판정이 안 바뀜 → **분량을 고쳤는데 통과하면 오히려 이상합니다.** 케이스가 그 성분을 안 쓰는 건 아닌지 확인하세요 |
| 일부 실패 | 실패 항목이 **개정 내용과 일치하는지** 하나씩 확인 |
| 오류 | 성분명이 표에 없거나 코드가 깨짐 |

실패 항목이 전부 의도한 변경이면 페이지 하단 `[기준값 새로 저장]` → 나온 내용으로 `tests/baseline.js` 전체 교체 → 다시 새로고침해서 전부 통과 확인.

> 확인 없이 기준값을 갱신하면 **버그를 정답으로 굳히게 됩니다.**

개정으로 판정이 달라지는 성분이 있다면 `tests/cases.js`에 **그 성분을 쓰는 케이스를 먼저 추가**하고 개정을 반영하는 편이 안전합니다. 변화가 눈에 보이기 때문입니다.

---

## 4. 알아두면 좋은 것

**허가목록 데이터** (`index.html`의 `_excelDB`, 150개 제품)는 표준제조기준과 무관합니다.
화면 상단 📂 버튼으로 최신 엑셀을 올려 갱신합니다.

**저장 제품**은 Firestore에 있습니다. 개정 후 기존 저장 제품을 다시 불러오면
새 기준으로 판정되므로, 이전에 적합이던 제품이 부적합이 될 수 있습니다. 정상 동작입니다.

**성분명 매칭**(허가목록 ↔ 표제기)은 `index.html`의 `ingrMatchLevel`에 있습니다.
괄호 별칭·표기 흔들림을 처리하되 `염산염` ≠ `질산염` 같은 다른 염은 구분합니다.
개정으로 성분명 표기가 바뀌면 `tests/cases.js`의 매칭 케이스도 함께 확인하세요.

## 화면에 안 나오는 장이 있습니다 (일부러 그런 것)

`app/표제기_데이터.js`에는 **14개 장**의 데이터가 다 들어 있지만,
화면에서 고를 수 있는 것은 **5개 장**뿐입니다.

```
화면에 나옴   제1장 비타민·미네랄 / 제2장 해열진통제 / 제3장 감기약
              제7장 진해거담제 / 제9장 비염용경구제

데이터만 있음 제4·5·6·8·10·11·12·13·14장
```

실제로 검토하시는 다섯 개 장만 열어 둔 것입니다.
데이터는 남겨 두었으니, **나중에 다른 장이 필요해지면 코드 한 줄만 고치면 됩니다.**

```
app/판정_규칙.js 의 CHAPTER_ORDER 에 장 이름을 추가
```

다만 장마다 배합 규칙이 달라서, 그 장의 `validateChapterN` 함수를 새로 만들어야 합니다.
**목록에 넣는 것만으로는 판정이 되지 않습니다.**

## 시험이 얇은 장

회귀 시험 51건의 분포입니다. 코드를 고친 뒤 `tests.html`을 열어 전부 통과하는지 보세요.

```
제1장 12건   제2장 11건   제3장 5건   제7장 1건 ←   제9장 3건
```

**제7장과 제9장은 시험이 얇습니다.** 이 두 장을 건드릴 때는 통과했다고 안심하지 마시고
화면에서 직접 확인해 주세요. 실제 허가 제품 예시가 생기면 `tests/cases.js`에 추가하면 됩니다.

## 원문과 데이터가 어긋나지 않았는지 확인하는 법

개정 때 항목을 빠뜨렸는지 **자동으로 확인**할 수 있습니다.

```bash
python 도구/원문대조.py
```

`참고자료/[별표 1] …표준제조기준.txt`(원문 전문)와 `app/표제기_데이터.js`를
대조해 **개수가 다른 절과 빠진 항목**을 `도구/원문대조_결과.txt`에 적어 줍니다.

읽는 법:

```
[제3장_감기약]
  유효성분의 분량    원문 12 / 데이터  9   ← 차이 -3
      빠짐  8) <표1>중 ⅩⅢ항에 기재된 …
```

`차이 -n`은 데이터에 n개가 없다는 뜻이고, `빠짐` 줄이 그 항목입니다.
`차이 +n`은 데이터가 더 많다는 뜻인데, 원문 한 항목을 둘로 나눠 적은
경우가 대부분이라 꼭 잘못은 아닙니다.

**주의 — 이 도구가 보는 것은 개수와 앞 글자뿐입니다.**
표(<표1>의 성분·수치)는 대조하지 않으므로, 차이가 0이라고 해서
표 내용까지 같다는 뜻은 아닙니다. 표는 여전히 눈으로 확인해야 합니다.

원문 파일이 CP949로 저장돼 있어 그냥 열면 글자가 깨집니다.
도구가 알아서 처리하니 직접 열지 말고 결과 파일을 보세요.
